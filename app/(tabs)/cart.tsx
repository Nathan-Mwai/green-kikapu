import {View, Text} from 'react-native'
import React from 'react'

/**
 * Renders the Cart tab UI.
 *
 * Displays a container with a label reading "Cart".
 *
 * @returns A React element representing the Cart tab.
 */
export default function Cart() {
    return (
        <View>
            <Text>Cart</Text>
        </View>
    )
}